type Venue {
    id: ID!
    externalId: FullExternalId!
    name: String!
}

input SaveVenueInput {
    name: String!
}

type VenueRecent {
    id: ID!
    externalId: FullExternalId!
    name: String!
    recentEvents(limit: Int = 5): [Event!]!
}